export class Universidade {
  nome: string;
  local: string;
  preco: number;

  constructor(nome: string, local: string, preco: number) {
    this.nome = nome;
    this.local = local;
    this.preco = preco;
  }
}
